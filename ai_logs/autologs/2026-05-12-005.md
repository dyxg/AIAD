### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 解释同一帖子之前已处理过时，当前系统会如何处理。
- **[Prompt]**: 用户询问“如果一个帖子之前就处理过了，会怎么办”。
- **[Rules Applied]**: 基于当前代码行为说明，不臆造不存在的跨任务去重能力；区分同任务内去重、历史任务缓存复用和未来可扩展策略。
- **[Skills Used]**: 后端任务缓存逻辑分析、标准化去重逻辑分析、任务结果一致性设计说明。
- **[Dev Process/Iterations]**: 说明当前 `normalize_dataset` 会在单次任务内按 `note_id` 去重帖子、按 `comment_id` 去重评论；但跨任务没有记录“该帖子已经处理过所以跳过”的状态。若相同关键词命中语义缓存，系统会复用旧 raw 文件生成新任务结果；若重新抓取又包含同一帖子，该帖子仍可能再次进入优先队列。
