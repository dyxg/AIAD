### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 为 `/api/ad-intel/task/{task_id}/insights` 的每条 review comment 增加 `likes` 和 `note_id` 字段。
- **[Prompt]**: 用户提出任务4前端新需求：`/api/ad-intel/task/{task_id}/insights` 返回值的每条评论需要新增 `likes`（点赞数，整数）和 `note_id`（帖子 id，字符串）。
- **[Rules Applied]**: 保持现有字段向后兼容，不移除 `comment_like_count`；仅扩展 review item 输出；不触发 MediaCrawler；不修改既有 `vendor/MediaCrawler` dirty 状态。
- **[Skills Used]**: FastAPI 响应组装、TypeScript 类型扩展、pytest、Vitest、Vite build。
- **[Dev Process/Iterations]**: 在 `_build_single_review_item` 中补充 `note_id` 和 `likes`，前端 `ReviewItem` 类型同步新增字段，并让 UI 优先显示 `likes`。补充路由测试断言。验证结果：`python -m pytest -q` 63 项通过，`npm test` 7 项通过，`npm run build` 成功，Python 相关 ruff 检查通过。
