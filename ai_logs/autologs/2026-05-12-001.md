### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 完整实现任务3的高赞帖子/高赞评论优先队列，前端改为刷新与复制文案，并补充文档和测试。
- **[Prompt]**: 用户要求不要使用 MediaCrawler，保留所有 AI 记录，完整完成任务3；需要记录、文档、单元测试、总体测试，并进一步要求选择策略解耦以便后期调整。
- **[Rules Applied]**: 未启动 MediaCrawler、未登录小红书、未实现或调用真实评论发送；保留原始 `comment_table`，新增优先队列而非破坏原数据；不覆盖已有 `vendor/MediaCrawler` 工作树改动；按项目规范补写 Auto-Log 文件。
- **[Skills Used]**: Python 3.10+、FastAPI、Pydantic、LangGraph 数据流、Vue 3、Vitest、pytest、可配置排序/筛选策略。
- **[Dev Process/Iterations]**: 先新增 `comment_selector` 模块生成 `priority_comment_table` 和 `comment_selection_meta`，并接入标准化后处理、LangGraph 输出、SSE 评论流、后台逐评论 AI 批处理和 insights 审核队列；随后将前端派发改为复制文案并增加刷新评论；再把策略抽象为 `CommentSelectionConfig`，支持后续替换帖子排序字段、评论点赞字段、意图词、首轮每帖配额和策略版本。补充任务3文档、README 说明、后端单元/集成测试和前端测试。验证结果：`python -m pytest -q` 通过 63 项，`npm test` 通过 6 项，`npm run build` 成功；未运行 MediaCrawler。`python -m ruff check app tests` 与 `python -m mypy app tests` 仍暴露仓库既有无关问题，本次相关文件的 `ruff` 检查已通过。
