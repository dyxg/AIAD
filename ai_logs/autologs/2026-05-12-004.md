### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 构造本地 mock 任务数据，并增加前端通过 `task_id` 查询参数加载历史任务的测试入口。
- **[Prompt]**: 用户要求“造点数据进去”，希望用本地已有/模拟缓存数据测试任务3效果，避免触发 MediaCrawler。
- **[Rules Applied]**: 只写入被 `.gitignore` 忽略的 `data/` 本地测试数据，不调用 `/api/ad-intel/run`，不启动 MediaCrawler，不登录小红书；保留生产路径的 MediaCrawler 调用能力。
- **[Skills Used]**: FastAPI task store 数据结构、JSON/JSONL fixture、Vue query 参数加载、Vitest、pytest。
- **[Dev Process/Iterations]**: 先确认本地 `data/tasks.json` 为空且 `data/` 被 git 忽略，再写入 `mock-priority-001` 的 raw JSONL、processed normalized JSON 和完成态 task record。随后验证 `/api/ad-intel/task/mock-priority-001/insights` 能返回 6 条优先审核队列，并在前端增加 `?task_id=mock-priority-001` 自动加载入口。验证结果：`npm test` 7 项通过，`npm run build` 成功，任务3相关后端测试 17 项通过。
