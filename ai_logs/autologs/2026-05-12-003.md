### 📝 AI 辅助开发自动记录 (Auto-Log)
- **[Task]**: 启动 AIAD 前端和后端服务，并说明任务3改动后系统整体行为。
- **[Prompt]**: 用户要求说明新增高赞评论优先队列后整个系统会怎么样，并打开前端查看。
- **[Rules Applied]**: 启动后端和前端但不主动触发 `/api/ad-intel/run`，因此不运行 MediaCrawler、不登录小红书；保留 MediaCrawler 的生产调用能力；记录本轮服务启动与验证结果。
- **[Skills Used]**: Vite dev server、Uvicorn/FastAPI、curl 服务探活、项目运行依赖安装。
- **[Dev Process/Iterations]**: 先确认 5173/8000 端口空闲，再启动前端 Vite 服务；后端首次启动缺少 requirements 中的 `apscheduler`，安装后重新启动成功。通过 `curl -I http://127.0.0.1:5173/` 验证前端返回 HTML，通过 `/api/ad-intel/agents/state-schema` 验证后端 API 正常。尝试用 Playwright 打开页面时发现本机缺 Chrome，安装 Chrome 需要 sudo 密码，因此无法生成截图；已提供可直接访问的本地 URL。
